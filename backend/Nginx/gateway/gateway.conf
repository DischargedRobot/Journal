
events {
    worker_connections 1024;
}

http {
    
    map $http_origin $cors_origin {
        default "";
        "http://localhost:3000" $http_origin;
    }

    upstream auth_services { 
        server auth-service:9000;
        keepalive 32;
    }

    upstream main_services {
        server main-service:9010;
        keepalive 32;
    }

    server {
        listen 9090;
        server_name api.journal.gateway;

        # CORS задаёт gateway; заголовки upstream убираем — иначе браузер видит дубликаты
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Credentials;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Expose-Headers;

        # subrequest для auth_request (не пересекается с публичным /auth/*)
        location = /_verify {
            internal;
            proxy_pass_request_body off;
            proxy_set_header Authorization $http_authorization;
            proxy_method POST;
            proxy_pass http://auth_services/auth-service/v1/Auth/check-authtoken;
        }

        location ^~ /api/auth/v1/Auth/register {
            # Preflight — без токена
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin  $cors_origin;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS";
                add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
                add_header Access-Control-Allow-Credentials "true";
                add_header Access-Control-Max-Age 86400;
                add_header Content-Length 0;
                return 204;
            }

            add_header Access-Control-Allow-Origin      $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;
            proxy_set_header Host $host;
            proxy_set_header Content-Type $content_type;
            proxy_http_version 1.1;
            proxy_pass http://auth_services/auth-service/v1/Auth/register;
        }
        location = /api/auth/v1/Auth/log-in {
                # Preflight — без токена
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin  $cors_origin;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS";
                add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
                add_header Access-Control-Allow-Credentials "true";
                add_header Access-Control-Max-Age 86400;
                add_header Content-Length 0;
                return 204;
            }

            add_header Access-Control-Allow-Origin      $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;
proxy_set_header Host $host;
proxy_set_header Content-Type $content_type;
proxy_http_version 1.1;
            proxy_pass http://auth_services/auth-service/v1/Auth/log-in;
        }

        # все /auth/* в auth_service без префикса /auth
        location ^~ /api/auth/v1 {
            # Preflight — без токена
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin  $cors_origin;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS";
                add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
                add_header Access-Control-Allow-Credentials "true";
                add_header Access-Control-Max-Age 86400;
                add_header Content-Length 0;
                return 204;
            }

            add_header Access-Control-Allow-Origin      $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;

            auth_request /_verify;
            auth_request_set $auth_json $upstream_http_x_error_json;
            auth_request_set $trace_id $upstream_http_x_trace_id;
            auth_request_set $user_id $upstream_http_x_user_id;

            proxy_set_header Authorization $http_authorization;
            proxy_set_header Host $host;
            proxy_set_header X-User-Id $user_id;
            proxy_set_header X-Trace-Id $trace_id;

            proxy_pass http://auth_services/auth-service/v1;

            error_page 401 = @err401;
        }

        location = /api/main/v1/groups {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin  $cors_origin;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS";
                add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
                add_header Access-Control-Allow-Credentials "true";
                add_header Access-Control-Max-Age 86400;
                add_header Content-Length 0;
                return 204;
            }

            error_page 418 = @public_groups;
            if ($request_method = GET) {
                return 418;
            }

            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_set_header Host $host;
            proxy_pass http://main_services/main-service/v1/Groups;
        }

        location = /api/main/v1/departments {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin  $cors_origin;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS";
                add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
                add_header Access-Control-Allow-Credentials "true";
                add_header Access-Control-Max-Age 86400;
                add_header Content-Length 0;
                return 204;
            }
            # если запрос на метод GET, то перенаправляем на public_groups
            error_page 418 = @public_departments;
            if ($request_method = GET) {
                return 418;
            }

            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_set_header Host $host;
            proxy_pass http://main_services/main-service/v1/Departments;
        }

        # все /main/* в main_service без префикса /main
        location ^~ /api/main/v1 {
            # Preflight — без токена
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin  $cors_origin;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS";
                add_header Access-Control-Allow-Headers "Authorization, Content-Type, Accept";
                add_header Access-Control-Allow-Credentials "true";
                add_header Access-Control-Max-Age 86400;
                add_header Content-Length 0;
                return 204;
            }

            # всегда разрешаем origin
            add_header Access-Control-Allow-Origin      $cors_origin always;
            # всегда разрешаем credentials
            add_header Access-Control-Allow-Credentials "true" always;

            auth_request /_verify;
            auth_request_set $auth_json $upstream_http_x_error_json;
            auth_request_set $trace_id $upstream_http_x_trace_id;
            auth_request_set $user_id $upstream_http_x_user_id;

            proxy_set_header Authorization $http_authorization;
            proxy_set_header Host $host;
            proxy_set_header X-User-Id $user_id;
            proxy_set_header X-Trace-Id $trace_id;

            proxy_pass http://main_services/main-service/v1;

            error_page 401 = @err401;
        }

        location @public_groups {
            internal;
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;
            proxy_set_header Host $host;
            rewrite ^ /main-service/v1/Groups break;
            proxy_pass http://main_services;
        }

        location @public_departments {
            internal;
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Credentials "true" always;
            proxy_set_header Host $host;
            rewrite ^ /main-service/v1/Departments break;
            proxy_pass http://main_services;
        }

        location @err401 {
            internal;
            default_type application/json;
            add_header X-Trace-Id $trace_id;
            return 401 $auth_json;
        }
    }
}